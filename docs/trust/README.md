Generated output. Owned by the `Trust Index` GitHub Actions workflow.

Do not hand-edit or commit local rebuilds — the scheduled job rewrites these files
daily and a local commit will collide with it. To test changes to the generator,
run `node scripts/build-index.mjs`, inspect the result, then `git checkout docs/trust`
before committing your source change.
